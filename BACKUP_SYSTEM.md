# 📦 نظام النسخ الاحتياطي التلقائي

## 📋 نظرة عامة

تم تطوير نظام نسخ احتياطي تلقائي ومتكامل لقاعدة البيانات مع واجهة إدارة شاملة.

---

## ✨ المميزات الرئيسية

### 1. النسخ الاحتياطي التلقائي
- 🕐 **جدولة يومية**: نسخ احتياطي تلقائي كل يوم الساعة 2:00 صباحاً
- 🗑️ **تنظيف تلقائي**: حذف النسخ الاحتياطية التلقائية الأقدم من 30 يوماً
- 🔄 **Cron Jobs**: مدمج مع Vercel Cron للتشغيل الموثوق

### 2. النسخ الاحتياطي اليدوي
- ⚡ **إنشاء فوري**: إنشاء نسخة احتياطية في أي وقت بضغطة زر واحدة
- 💾 **تنزيل مباشر**: تنزيل النسخ الاحتياطية على جهازك
- 🎯 **الاحتفاظ الدائم**: النسخ اليدوية لا تُحذف تلقائياً

### 3. واجهة إدارة متقدمة
- 📊 **إحصائيات شاملة**: عدد النسخ، الحجم الإجمالي، آخر نسخة
- 📋 **قائمة تفصيلية**: عرض جميع النسخ مع حالتها ونوعها
- 🔍 **حالات متعددة**: completed, failed, in_progress
- 🌙 **Dark Mode**: دعم كامل للوضع الليلي

### 4. التحكم الكامل
- ⬇️ **تنزيل**: تحميل أي نسخة احتياطية
- 🗑️ **حذف**: إزالة النسخ غير المرغوبة
- 🧹 **تنظيف**: حذف دفعي للنسخ القديمة
- 🔄 **تحديث**: تحديث القائمة لحظياً

---

## 🏗️ البنية التقنية

### 1. قاعدة البيانات

#### جدول Backup
```prisma
model Backup {
  id        String   @id @default(cuid())
  filename  String
  size      BigInt   // حجم الملف بالبايت
  type      String   @default("automatic") // manual, automatic
  status    String   @default("in_progress") // completed, failed, in_progress
  error     String?  // رسالة الخطأ في حالة الفشل
  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([status])
}
```

**الحقول:**
- `id`: معرّف فريد
- `filename`: اسم ملف النسخة
- `size`: حجم الملف (BigInt للملفات الكبيرة)
- `type`: نوع النسخة (manual/automatic)
- `status`: حالة العملية
- `error`: رسالة الخطأ إذا فشلت
- `createdAt`: تاريخ الإنشاء

**الفهارس:**
- `createdAt`: لترتيب النسخ حسب التاريخ
- `status`: لفلترة النسخ حسب الحالة

### 2. المكتبة الأساسية

**الملف:** `src/lib/backup.ts`

#### الدوال الرئيسية:

##### createDatabaseBackup()
```typescript
async function createDatabaseBackup(type: 'manual' | 'automatic'): Promise<BackupInfo>
```
- ينشئ نسخة احتياطية من قاعدة البيانات باستخدام `pg_dump`
- يحفظ الملف في مجلد `backups/`
- يسجل المعلومات في جدول Backup
- يُرجع معلومات النسخة المُنشأة

##### getBackups()
```typescript
async function getBackups(): Promise<BackupInfo[]>
```
- يجلب قائمة بآخر 50 نسخة احتياطية
- مرتبة حسب التاريخ (الأحدث أولاً)
- يحول BigInt إلى Number

##### deleteBackup()
```typescript
async function deleteBackup(backupId: string): Promise<boolean>
```
- يحذف النسخة من نظام الملفات
- يحذف السجل من قاعدة البيانات
- يُرجع true إذا نجح

##### cleanupOldBackups()
```typescript
async function cleanupOldBackups(daysToKeep: number = 30): Promise<number>
```
- يحذف النسخ التلقائية الأقدم من X يوم
- النسخ اليدوية محمية من الحذف
- يُرجع عدد النسخ المحذوفة

##### getBackupFile()
```typescript
async function getBackupFile(backupId: string): Promise<Buffer | null>
```
- يقرأ ملف النسخة الاحتياطية
- يُرجع Buffer للتنزيل
- يُرجع null إذا لم يوجد الملف

##### getBackupStats()
```typescript
async function getBackupStats()
```
- يحسب إحصائيات شاملة:
  - إجمالي النسخ
  - الناجحة/الفاشلة
  - الحجم الإجمالي
  - آخر نسخة احتياطية

### 3. API Endpoints

#### GET /api/backups
**الاستخدامات:**
1. `GET /api/backups` - جلب قائمة النسخ
2. `GET /api/backups?action=stats` - جلب الإحصائيات
3. `GET /api/backups?action=download&id={backupId}` - تنزيل نسخة

**الصلاحيات:** ADMIN, GENERAL_MANAGER فقط

**الاستجابات:**
```json
// قائمة النسخ
{
  "backups": [
    {
      "id": "clxxx",
      "filename": "backup-2025-01-09T14-23-45.sql",
      "size": 2048576,
      "createdAt": "2025-01-09T14:23:45Z",
      "type": "automatic",
      "status": "completed"
    }
  ]
}

// الإحصائيات
{
  "total": 25,
  "successful": 23,
  "failed": 2,
  "totalSizeBytes": 52428800,
  "totalSizeMB": "50.00",
  "lastBackup": {
    "date": "2025-01-09T02:00:00Z",
    "filename": "backup-2025-01-09T02-00-00.sql"
  }
}
```

#### POST /api/backups
**الإجراءات:**
1. `create` - إنشاء نسخة احتياطية يدوية
2. `delete` - حذف نسخة
3. `cleanup` - تنظيف النسخ القديمة

**الأمثلة:**
```json
// إنشاء نسخة
{
  "action": "create"
}

// حذف نسخة
{
  "action": "delete",
  "backupId": "clxxx"
}

// تنظيف
{
  "action": "cleanup",
  "daysToKeep": 30
}
```

#### POST/GET /api/backups/scheduled
**النسخ الاحتياطي المجدول (Cron)**

- يتم استدعاؤه من Vercel Cron
- يتطلب Authorization header: `Bearer {CRON_SECRET}`
- ينشئ نسخة احتياطية تلقائية
- ينظف النسخ القديمة (أقدم من 30 يوم)

**الجدولة:**
```json
{
  "path": "/api/backups/scheduled",
  "schedule": "0 2 * * *"  // كل يوم 2:00 صباحاً
}
```

### 4. واجهة المستخدم

**الصفحة:** `src/app/(dashboard)/backups/page.tsx`

#### المكونات:

##### بطاقات الإحصائيات
- إجمالي النسخ
- النسخ الناجحة
- النسخ الفاشلة
- الحجم الكلي بالـ MB

##### بطاقة آخر نسخة احتياطية
- التاريخ والوقت
- اسم الملف
- تصميم بارز

##### أزرار الإجراءات
- **إنشاء نسخة احتياطية الآن**: زر أزرق مع أيقونة Database
- **تنظيف النسخ القديمة**: زر برتقالي للحذف الدفعي
- **تحديث**: زر رمادي لإعادة تحميل البيانات

##### تنبيه النسخ التلقائي
- معلومات الجدولة
- تذكير بحذف النسخ القديمة

##### جدول النسخ الاحتياطية
**الأعمدة:**
1. الحالة (أيقونة + نص)
2. اسم الملف (خط Mono)
3. النوع (badge ملون)
4. الحجم (MB)
5. التاريخ (تنسيق عربي)
6. الإجراءات (تنزيل + حذف)

**المميزات:**
- Hover effects
- أيقونات حالة ملونة
- Badges للنوع
- أزرار تنزيل/حذف سريعة

---

## 🔧 الإعداد والتهيئة

### 1. متغيرات البيئة
```env
DATABASE_URL="postgresql://user:pass@host:5432/dbname"
CRON_SECRET="your-random-secret-key"  # للحماية
```

### 2. التثبيت
```bash
# 1. تطبيق Migration
npx prisma migrate dev

# 2. توليد Prisma Client
npx prisma generate

# 3. تشغيل الخادم
npm run dev
```

### 3. متطلبات النظام
- PostgreSQL database
- `pg_dump` و `psql` مثبتة في النظام
- صلاحيات الكتابة على مجلد `backups/`
- Vercel Cron (للإنتاج)

---

## 📱 الاستخدام

### للمستخدم العادي:

#### 1. الوصول للصفحة
- من القائمة الجانبية: **الإدارة** → **النسخ الاحتياطية**
- الصلاحيات المطلوبة: ADMIN أو GENERAL_MANAGER

#### 2. إنشاء نسخة احتياطية يدوياً
1. اضغط زر "إنشاء نسخة احتياطية الآن"
2. انتظر حتى تنتهي العملية
3. ستظهر رسالة نجاح وتُحدث القائمة

#### 3. تنزيل نسخة احتياطية
1. ابحث عن النسخة المرغوبة في الجدول
2. اضغط أيقونة التنزيل (⬇️)
3. سيبدأ التنزيل تلقائياً

#### 4. حذف نسخة
1. اضغط أيقونة الحذف (🗑️)
2. أكّد الحذف في النافذة المنبثقة
3. ستُحذف النسخة فوراً

#### 5. تنظيف النسخ القديمة
1. اضغط "تنظيف النسخ القديمة"
2. أكّد العملية
3. سيتم حذف النسخ التلقائية الأقدم من 30 يوم

### للمطور:

#### استخدام مكتبة backup
```typescript
import { 
  createDatabaseBackup, 
  getBackups, 
  deleteBackup 
} from '@/lib/backup';

// إنشاء نسخة احتياطية
const backup = await createDatabaseBackup('manual');
console.log(backup.filename);

// جلب القائمة
const backups = await getBackups();

// حذف نسخة
await deleteBackup('clxxx');
```

#### تخصيص الجدولة
في `vercel.json`:
```json
{
  "crons": [
    {
      "path": "/api/backups/scheduled",
      "schedule": "0 2 * * *"  // غيّر حسب الحاجة
    }
  ]
}
```

**أمثلة الجدولة:**
- `0 2 * * *` - كل يوم الساعة 2:00 صباحاً
- `0 */6 * * *` - كل 6 ساعات
- `0 0 * * 0` - كل أحد منتصف الليل

---

## 🔒 الأمان

### 1. الصلاحيات
- فقط ADMIN و GENERAL_MANAGER يمكنهم:
  - إنشاء نسخ احتياطية
  - تنزيل النسخ
  - حذف النسخ
  - عرض الإحصائيات

### 2. حماية Cron Job
- يتطلب Authorization header
- CRON_SECRET مخزن في متغيرات البيئة
- لا يمكن الوصول بدون التوكن

### 3. حماية الملفات
- الملفات مخزنة خارج مجلد public
- لا يمكن الوصول المباشر للملفات
- التنزيل فقط عبر API

---

## 🚨 استكشاف الأخطاء

### مشكلة: "pg_dump command not found"
**الحل:**
```bash
# Windows
choco install postgresql

# macOS
brew install postgresql

# Linux
sudo apt-get install postgresql-client
```

### مشكلة: "Permission denied"
**الحل:**
```bash
# إنشاء مجلد backups مع الصلاحيات المناسبة
mkdir -p backups
chmod 755 backups
```

### مشكلة: "Database connection failed"
**الحل:**
- تحقق من DATABASE_URL في `.env`
- تأكد من الاتصال بقاعدة البيانات
- جرب الاتصال يدوياً: `psql $DATABASE_URL`

### مشكلة: "Cron job not running"
**الحل:**
- تأكد من نشر المشروع على Vercel
- تحقق من CRON_SECRET في Vercel Environment Variables
- راجع سجلات Vercel Cron

---

## 📊 الإحصائيات والتقارير

### معلومات تُعرض:
1. **إجمالي النسخ**: العدد الكلي لجميع النسخ
2. **الناجحة**: النسخ المكتملة بنجاح
3. **الفاشلة**: النسخ التي فشلت
4. **الحجم الكلي**: مجموع أحجام الملفات بالـ MB
5. **آخر نسخة**: تاريخ واسم آخر نسخة احتياطية

### رسائل الحالة:
- ✅ **مكتمل** (completed): نسخة ناجحة
- ❌ **فشل** (failed): خطأ في العملية
- ⏳ **جاري...** (in_progress): قيد التنفيذ

---

## 🎨 التصميم

### الألوان:
- **إجمالي النسخ**: Indigo (#6366F1)
- **الناجحة**: Green (#10B981)
- **الفاشلة**: Red (#EF4444)
- **الحجم الكلي**: Purple (#A855F7)

### التأثيرات:
- **Glass Effect**: خلفية شفافة ضبابية
- **Hover**: تغيير الخلفية عند التمرير
- **Shadows**: ظلال ناعمة للبطاقات
- **Transitions**: انتقالات سلسة للتفاعلات

### Dark Mode:
- دعم كامل للوضع الليلي
- ألوان محسّنة للقراءة
- تباين مناسب

---

## 🔄 سير العمل التلقائي

### 1. Cron Job اليومي (2:00 AM)
```
1. Vercel Cron يُشغّل /api/backups/scheduled
2. التحقق من Authorization
3. استدعاء createDatabaseBackup('automatic')
4. إنشاء النسخة وحفظها
5. استدعاء cleanupOldBackups(30)
6. حذف النسخ الأقدم من 30 يوم
7. إرجاع تقرير بالنتائج
```

### 2. النسخ اليدوي
```
1. المستخدم يضغط "إنشاء نسخة احتياطية"
2. POST /api/backups { action: 'create' }
3. createDatabaseBackup('manual')
4. إنشاء النسخة
5. تحديث الواجهة
6. عرض رسالة نجاح
```

### 3. التنظيف اليدوي
```
1. المستخدم يضغط "تنظيف النسخ القديمة"
2. تأكيد الحذف
3. POST /api/backups { action: 'cleanup', daysToKeep: 30 }
4. cleanupOldBackups(30)
5. حذف النسخ التلقائية القديمة
6. عرض عدد النسخ المحذوفة
```

---

## 📝 ملاحظات مهمة

### ⚠️ تحذيرات:
1. **النسخ اليدوية**: لا تُحذف تلقائياً، احذفها يدوياً عند الحاجة
2. **حجم الملفات**: قد تكبر النسخ الاحتياطية مع نمو البيانات
3. **المساحة**: تأكد من وجود مساحة كافية في الخادم
4. **الأداء**: عملية النسخ قد تستغرق دقائق للبيانات الكبيرة

### ✅ أفضل الممارسات:
1. احتفظ بـ 3-5 نسخ يدوية على الأقل
2. نظّف النسخ القديمة شهرياً
3. اختبر عملية الاستعادة دورياً
4. راقب حجم مجلد backups
5. احتفظ بنسخة خارجية في مكان آمن

### 💡 نصائح:
- استخدم أسماء واضحة للنسخ اليدوية
- اضغط "تحديث" بعد كل عملية
- راجع الإحصائيات أسبوعياً
- تأكد من نجاح Cron Jobs في Vercel

---

## 🚀 التطوير المستقبلي

### مقترحات:
1. ☁️ رفع النسخ إلى السحابة (S3, Google Cloud)
2. 📧 إشعارات بريد إلكتروني عند الفشل
3. 🔄 استعادة تلقائية للبيانات
4. 📊 رسوم بيانية لحجم النسخ
5. 🗜️ ضغط النسخ الاحتياطية
6. 🔐 تشفير الملفات
7. ⏰ جدولة مخصصة لكل مستخدم
8. 📱 تطبيق موبايل لإدارة النسخ

---

## 📞 الدعم

في حالة وجود مشاكل:
1. راجع قسم "استكشاف الأخطاء"
2. تحقق من سجلات الخادم
3. تأكد من الصلاحيات والإعدادات
4. راجع توثيق Prisma و PostgreSQL

---

**تم التطوير بواسطة:** نظام ساعد HR
**التاريخ:** يناير 2025
**الإصدار:** 1.0.0

✅ **جاهز للاستخدام في الإنتاج!**
