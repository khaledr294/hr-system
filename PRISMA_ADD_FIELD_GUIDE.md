# دليل إضافة حقل medicalStatus عبر Prisma Studio

## الطريقة السهلة (موصى بها) ✅

### الخطوات:

1. **افتح Prisma Console**
   - اذهب إلى: https://console.prisma.io
   - سجل دخولك

2. **اختر مشروعك**
   - اضغط على المشروع الخاص بـ HR System

3. **افتح Data Browser**
   - من القائمة الجانبية، اضغط على **"Data Browser"** أو **"Browse Data"**

4. **اختر جدول Worker**
   - من قائمة الجداول في اليسار، اضغط على **"Worker"**

5. **أضف الحقل الجديد**
   
   **الطريقة A: عبر الواجهة (إذا توفرت)**
   - ابحث عن زر **"Add field"** أو **"+ Add column"** في أعلى الجدول
   - املأ التفاصيل:
     * **Field name**: `medicalStatus`
     * **Type**: `String` أو `Text`
     * **Default value**: `PENDING_REPORT`
     * **Optional/Nullable**: نعم ✓
   - اضغط **"Save"** أو **"Apply Changes"**

   **الطريقة B: عبر Query Console**
   - إذا لم تجد خيار Add field، ابحث عن **"Query Console"** أو **"SQL Editor"**
   - نفذ هذا الأمر:
   ```sql
   ALTER TABLE "Worker" 
   ADD COLUMN "medicalStatus" TEXT DEFAULT 'PENDING_REPORT';
   ```
   - اضغط **"Run"** أو **"Execute"**

6. **تحقق من النجاح**
   - تحديث صفحة Worker في Data Browser
   - يجب أن ترى العمود الجديد `medicalStatus`

7. **تفعيل الحقل في النظام**
   - ارجع لمشروعك في VS Code
   - في ملف `prisma/schema.prisma`، أضف السطر التالي في `model Worker`:
   ```prisma
   medicalStatus       String?            @default("PENDING_REPORT")
   ```
   - في terminal، نفذ:
   ```bash
   npx prisma generate
   ```

8. **إلغاء تعطيل الحقل في الكود**
   - افتح الملفات التالية وأزل التعليقات `//` من أمام `medicalStatus`:
     * `src/app/workers/new/page.tsx`
     * `src/app/workers/[id]/edit/page.tsx`
     * `src/app/api/workers/route.ts`

9. **ارفع التعديلات**
   ```bash
   git add -A
   git commit -m "feat: تفعيل حقل medicalStatus"
   git push origin main
   ```

---

## إذا واجهت صعوبة 🤔

### لا تجد Query Console؟

بعض الواجهات في Prisma قد تختلف. جرب:
- **Settings** → **Database Access**
- **Tools** → **Query Console** 
- **Schema** → **SQL Editor**

### لا يمكنك إضافة الحقل؟

يمكنك:
1. التواصل مع دعم Prisma عبر: https://www.prisma.io/support
2. طلب المساعدة في Discord: https://pris.ly/discord
3. استخدام Prisma CLI المحلي (لكن يحتاج direct database URL)

### البديل: استخدام Prisma Migrate (متقدم)

إذا كان لديك `DIRECT_URL` في `.env`:
```bash
npx prisma migrate dev --name add_medical_status
```

---

## التحقق من النجاح ✅

بعد إضافة الحقل:
1. اذهب إلى: `http://localhost:3000/admin/migrate`
2. اضغط **"تشغيل Migration"**
3. يجب أن تظهر رسالة: "Column medicalStatus already exists"

---

## ملاحظات مهمة

- ⚠️ **لا تحذف الحقل بعد إضافته** - حتى لو كان فارغاً
- ✅ **القيمة الافتراضية** `PENDING_REPORT` ستطبق على جميع العاملات الحاليات
- 📝 **الحقل اختياري** - لن يسبب مشاكل إذا كان فارغاً
- 🔄 **يمكنك تحديثه لاحقاً** من صفحة تعديل العاملة

---

## في حالة الطوارئ 🚨

إذا لم تستطع إضافة الحقل، النظام سيعمل بدونه:
- ✅ إضافة وتعديل العاملات يعمل
- ✅ إدارة حالة العاملة (مريضة/هاربة) يعمل
- ❌ فقط حقل "حالة الفحص الطبي" لن يظهر

يمكنك إضافته في أي وقت لاحق بدون مشاكل!
