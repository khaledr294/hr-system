# 📋 دليل الصلاحيات الشامل - نظام إدارة الموارد البشرية

## إجمالي الصلاحيات: **36 صلاحية**

تم تحديث النظام ليشمل جميع الـ 36 صلاحية المتاحة في قاعدة البيانات.

---

## 📊 الصلاحيات حسب الفئة

### 👷 العمال (5 صلاحيات)
| الصلاحية | الوصف | الاستخدام |
|---------|-------|----------|
| `VIEW_WORKERS` | عرض بيانات العاملات | صفحة العاملات، البحث |
| `CREATE_WORKERS` | إضافة عاملات جديدة | نموذج إضافة عاملة |
| `EDIT_WORKERS` | تعديل بيانات العاملات | تحديث معلومات العاملة |
| `DELETE_WORKERS` | حذف العاملات | إزالة عاملة من النظام |
| `RESERVE_WORKERS` | حجز العاملات | نظام الحجز المؤقت |

### 📄 العقود (4 صلاحيات)
| الصلاحية | الوصف | الاستخدام |
|---------|-------|----------|
| `VIEW_CONTRACTS` | عرض العقود | صفحة العقود، التفاصيل |
| `CREATE_CONTRACTS` | إنشاء عقود جديدة | نموذج عقد جديد |
| `EDIT_CONTRACTS` | تعديل العقود | تحديث بيانات العقد |
| `DELETE_CONTRACTS` | حذف العقود | إلغاء عقد |

### 👥 العملاء (4 صلاحيات)
| الصلاحية | الوصف | الاستخدام |
|---------|-------|----------|
| `VIEW_CLIENTS` | عرض العملاء | صفحة العملاء |
| `CREATE_CLIENTS` | إضافة عملاء جدد | نموذج عميل جديد |
| `EDIT_CLIENTS` | تعديل بيانات العملاء | تحديث معلومات العميل |
| `DELETE_CLIENTS` | حذف العملاء | إزالة عميل |

### 🔐 المستخدمين (4 صلاحيات)
| الصلاحية | الوصف | الاستخدام |
|---------|-------|----------|
| `VIEW_USERS` | عرض المستخدمين | صفحة المستخدمين |
| `CREATE_USERS` | إضافة مستخدمين | إنشاء حساب جديد |
| `EDIT_USERS` | تعديل المستخدمين | تحديث بيانات المستخدم |
| `DELETE_USERS` | حذف المستخدمين | إزالة مستخدم |

### 📊 التقارير (3 صلاحيات)
| الصلاحية | الوصف | الاستخدام |
|---------|-------|----------|
| `VIEW_REPORTS` | عرض التقارير | الوصول لصفحة التقارير |
| `MANAGE_REPORTS` | إدارة التقارير | إنشاء وتخصيص التقارير |
| `EXPORT_DATA` | تصدير البيانات | تصدير Excel/PDF |

### 💰 كشف الرواتب (4 صلاحيات)
| الصلاحية | الوصف | الاستخدام |
|---------|-------|----------|
| `VIEW_PAYROLL` | عرض كشف الرواتب | صفحة الرواتب |
| `MANAGE_PAYROLL` | إدارة كشف الرواتب | تعديل وإصدار الرواتب |
| `VIEW_PAYROLL_DELIVERY` | عرض توصيل الرواتب | متابعة التوصيل |
| `MANAGE_PAYROLL_DELIVERY` | إدارة توصيل الرواتب | تحديث حالة التوصيل |

### ⚙️ النظام (12 صلاحية)
| الصلاحية | الوصف | الاستخدام |
|---------|-------|----------|
| `VIEW_LOGS` | عرض السجلات | صفحة سجل العمليات |
| `MANAGE_SETTINGS` | إدارة الإعدادات | إعدادات النظام |
| `MANAGE_JOB_TITLES` | إدارة المسميات الوظيفية | صفحة المسميات |
| `VIEW_BACKUPS` | عرض النسخ الاحتياطية | صفحة النسخ |
| `MANAGE_BACKUPS` | إدارة النسخ الاحتياطية | إنشاء واستعادة |
| `VIEW_ARCHIVE` | عرض الأرشيف | صفحة الأرشيف |
| `MANAGE_ARCHIVE` | إدارة الأرشيف | أرشفة واستعادة |
| `MANAGE_PACKAGES` | إدارة الباقات | إدارة باقات العقود |
| `MANAGE_TEMPLATES` | إدارة القوالب | قوالب العقود |
| `VIEW_PERFORMANCE` | عرض الأداء | لوحة الأداء |
| `VIEW_SEARCH` | البحث المتقدم | البحث الشامل |

---

## 🔒 آلية عمل الصلاحيات

### 1. **المسميات الوظيفية (Job Titles)**
- يتم تعيين الصلاحيات للمسمى الوظيفي
- المستخدم يرث صلاحيات مسماه الوظيفي
- يمكن تعديل صلاحيات المسمى في أي وقت

### 2. **حماية API Routes**
```typescript
export const GET = withApiAuth(
  { permissions: [Permission.VIEW_WORKERS] },
  async ({ req }) => {
    // API logic
  }
);
```

### 3. **حماية الصفحات**
```typescript
const session = await getServerSession(authOptions);
if (!hasPermission(session, Permission.VIEW_WORKERS)) {
  redirect('/403');
}
```

### 4. **التحقق المتعدد**
- `hasAllPermissions`: يجب توفر جميع الصلاحيات
- `hasAnyPermission`: يكفي توفر صلاحية واحدة

---

## ✅ التحقق من الصلاحيات

### في Schema.prisma
```prisma
enum Permission {
  VIEW_WORKERS
  CREATE_WORKERS
  ... (36 صلاحية)
}
```

### في permissions.ts
```typescript
export const PERMISSION_LABELS: Record<Permission, string> = {
  VIEW_WORKERS: "عرض بيانات العاملات",
  ... (36 ترجمة)
}
```

### في job-titles/page.tsx
```typescript
const AVAILABLE_PERMISSIONS = [
  { id: "VIEW_WORKERS", label: "عرض العمال", category: "workers" },
  ... (36 صلاحية)
];
```

---

## 📈 إحصائيات الاستخدام

### الصلاحيات الأكثر استخداماً:
1. `VIEW_WORKERS` - 15 موقع
2. `VIEW_CONTRACTS` - 12 موقع
3. `VIEW_CLIENTS` - 10 مواقع
4. `MANAGE_SETTINGS` - 8 مواقع
5. `VIEW_REPORTS` - 7 مواقع

### API Routes المحمية:
- ✅ جميع الـ 60+ API route محمية بالصلاحيات
- ✅ جميع الصفحات الحساسة محمية
- ✅ تسجيل تلقائي لمحاولات الوصول المرفوضة

---

## 🎯 أمثلة على المسميات الوظيفية

### مدير النظام (Admin)
```
جميع الـ 36 صلاحية
```

### مسوق
```
VIEW_WORKERS
CREATE_WORKERS
EDIT_WORKERS
RESERVE_WORKERS
VIEW_CONTRACTS
CREATE_CONTRACTS
EDIT_CONTRACTS
VIEW_CLIENTS
CREATE_CLIENTS
EDIT_CLIENTS
```

### محاسب
```
VIEW_WORKERS
VIEW_CONTRACTS
VIEW_CLIENTS
VIEW_REPORTS
MANAGE_REPORTS
EXPORT_DATA
VIEW_PAYROLL
MANAGE_PAYROLL
```

### مشاهد فقط (Viewer)
```
VIEW_WORKERS
VIEW_CONTRACTS
VIEW_CLIENTS
VIEW_REPORTS
```

---

## 🔄 التحديثات الأخيرة

### تاريخ 17/11/2025
- ✅ إضافة 13 صلاحية جديدة (من 23 إلى 36)
- ✅ إضافة فئة "كشف الرواتب" (4 صلاحيات)
- ✅ تحديث واجهة المسميات الوظيفية
- ✅ التحقق من عمل جميع الصلاحيات بكفاءة
- ✅ إضافة ترجمات عربية لجميع الصلاحيات

### الصلاحيات الجديدة المضافة:
1. `MANAGE_REPORTS` - إدارة التقارير
2. `VIEW_PAYROLL` - عرض كشف الرواتب
3. `MANAGE_PAYROLL` - إدارة كشف الرواتب
4. `VIEW_PAYROLL_DELIVERY` - عرض توصيل الرواتب
5. `MANAGE_PAYROLL_DELIVERY` - إدارة توصيل الرواتب
6. `VIEW_BACKUPS` - عرض النسخ الاحتياطية
7. `MANAGE_BACKUPS` - إدارة النسخ الاحتياطية
8. `VIEW_ARCHIVE` - عرض الأرشيف
9. `MANAGE_ARCHIVE` - إدارة الأرشيف
10. `MANAGE_PACKAGES` - إدارة الباقات
11. `MANAGE_TEMPLATES` - إدارة القوالب
12. `VIEW_PERFORMANCE` - عرض الأداء
13. `VIEW_SEARCH` - البحث المتقدم

---

## 📝 ملاحظات مهمة

1. **الأمان**:
   - جميع API routes محمية بنظام `withApiAuth`
   - تسجيل تلقائي لمحاولات الوصول غير المصرح بها
   - تحديث فوري للصلاحيات عند تعديل المسمى الوظيفي

2. **الأداء**:
   - التحقق من الصلاحيات يتم في الذاكرة (سريع جداً)
   - لا يوجد استعلامات إضافية للقاعدة عند كل طلب
   - الصلاحيات محفوظة في جلسة المستخدم

3. **الصيانة**:
   - إضافة صلاحية جديدة تتطلب:
     * إضافتها في `schema.prisma`
     * إضافة ترجمتها في `permissions.ts`
     * إضافتها في `job-titles/page.tsx`
     * تشغيل `prisma generate` و `prisma migrate`

---

## 🚀 الاستخدام السريع

### إنشاء مسمى وظيفي جديد:
1. اذهب إلى صفحة "المسميات الوظيفية"
2. اضغط "إضافة مسمى وظيفي جديد"
3. أدخل الاسم والوصف
4. اختر الصلاحيات من القائمة (36 صلاحية متاحة)
5. احفظ

### تعيين مستخدم لمسمى:
1. اذهب إلى صفحة "المستخدمين"
2. اختر المستخدم
3. اختر المسمى الوظيفي
4. سيحصل المستخدم على جميع صلاحيات المسمى تلقائياً

---

**تم التحديث**: 17 نوفمبر 2025  
**النسخة**: Beta v0.1  
**المطور**: نظام إدارة الموارد البشرية
